<div class="product-details">
    <!-- En-tête produit -->
    <div class="product-header mb-4">
        <div class="d-flex justify-content-between align-items-start">
            <div>
                <h2 class="product-title mb-2"><?= htmlspecialchars($product['name']) ?></h2>
                <div class="product-meta">
                    <span class="badge bg-light text-dark me-2">Réf: <?= htmlspecialchars($product['reference']) ?></span>
                    <?php if (!empty($product['barcode'])): ?>
                    <span class="badge bg-light text-dark">Code: <?= htmlspecialchars($product['barcode']) ?></span>
                    <?php endif; ?>
                </div>
            </div>
            <div class="stock-indicator">
                <span class="badge bg-<?= $product['stock_status']['class'] ?> fs-6">
                    <i class="bi bi-<?= $product['stock_status']['icon'] ?>"></i>
                    <?= $product['stock_status']['label'] ?>
                </span>
                <?php if ($product['quantity'] > 0): ?>
                <div class="text-muted small mt-1"><?= $product['quantity'] ?> unités disponibles</div>
                <?php endif; ?>
            </div>
        </div>
    </div>

    <div class="row">
        <!-- Galerie d'images -->
        <div class="col-md-6 mb-4">
            <div class="product-gallery">
                <?php if (count($product['images']) > 1): ?>
                <!-- Image principale -->
                <div class="main-image-container mb-3">
                    <img src="<?= htmlspecialchars($product['images'][0]['url']) ?>" 
                         alt="<?= htmlspecialchars($product['images'][0]['alt']) ?>"
                         id="mainProductImage"
                         class="img-fluid rounded main-product-image"
                         style="width: 100%; height: 350px; object-fit: contain; background: #f8f9fa;">
                </div>
                
                <!-- Miniatures -->
                <div class="thumbnails-container">
                    <div class="d-flex gap-2 overflow-auto">
                        <?php foreach ($product['images'] as $index => $image): ?>
                        <img src="<?= htmlspecialchars($image['url']) ?>" 
                             alt="<?= htmlspecialchars($image['alt']) ?>"
                             class="thumbnail-image <?= $index === 0 ? 'active' : '' ?>"
                             data-index="<?= $index ?>"
                             onclick="changeMainImage('<?= htmlspecialchars($image['url']) ?>', <?= $index ?>)"
                             style="width: 80px; height: 80px; object-fit: cover; cursor: pointer; border-radius: 6px; border: 2px solid transparent; transition: all 0.2s;">
                        <?php endforeach; ?>
                    </div>
                </div>
                <?php else: ?>
                <!-- Image unique -->
                <div class="single-image-container">
                    <img src="<?= htmlspecialchars($product['images'][0]['url']) ?>" 
                         alt="<?= htmlspecialchars($product['images'][0]['alt']) ?>"
                         class="img-fluid rounded"
                         style="width: 100%; height: 350px; object-fit: contain; background: #f8f9fa;">
                </div>
                <?php endif; ?>
            </div>
        </div>

        <!-- Informations produit -->
        <div class="col-md-6">
            <!-- Prix principal -->
            <div class="price-section mb-4">
                <div class="main-price display-5 fw-bold text-primary mb-2">
                    <?= $product['formatted_price'] ?>
                </div>
                
                <?php if (($product['cout_de_revient_unitaire'] ?? 0) > 0 || ($product['prix_min_vente'] ?? 0) > 0): ?>
                <div class="price-breakdown">
                    <div class="row g-2">
                        <?php if (($product['cout_de_revient_unitaire'] ?? 0) > 0): ?>
                        <div class="col-4">
                            <div class="card bg-light border-0">
                                <div class="card-body p-2 text-center">
                                    <small class="text-muted d-block">Coût revient</small>
                                    <strong class="small"><?= $product['formatted_cost'] ?></strong>
                                </div>
                            </div>
                        </div>
                        <?php endif; ?>
                        
                        <?php if (($product['prix_min_vente'] ?? 0) > 0): ?>
                        <div class="col-4">
                            <div class="card bg-light border-0">
                                <div class="card-body p-2 text-center">
                                    <small class="text-muted d-block">Prix min</small>
                                    <strong class="small"><?= $product['formatted_min_price'] ?></strong>
                                </div>
                            </div>
                        </div>
                        <?php endif; ?>
                        
                        <div class="col-4">
                            <div class="card bg-primary text-white border-0">
                                <div class="card-body p-2 text-center">
                                    <small class="d-block opacity-75">Prix conseillé</small>
                                    <strong class="small"><?= $product['formatted_price'] ?></strong>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <?php endif; ?>
            </div>

            <!-- Informations détaillées -->
            <div class="product-info">
                <div class="card border-0 bg-light mb-3">
                    <div class="card-body">
                        <h5 class="card-title h6 mb-3">
                            <i class="bi bi-info-circle text-primary"></i> Caractéristiques
                        </h5>
                        
                        <div class="row g-2">
                            <div class="col-sm-6">
                                <strong class="small text-muted">Référence:</strong>
                                <div><?= htmlspecialchars($product['reference']) ?></div>
                            </div>
                            
                            <?php if (!empty($product['label'])): ?>
                            <div class="col-sm-6">
                                <strong class="small text-muted">Label:</strong>
                                <div><?= htmlspecialchars($product['label']) ?></div>
                            </div>
                            <?php endif; ?>
                            
                            <?php if (!empty($product['barcode'])): ?>
                            <div class="col-sm-6">
                                <strong class="small text-muted">Code-barres:</strong>
                                <div class="font-monospace small"><?= htmlspecialchars($product['barcode']) ?></div>
                            </div>
                            <?php endif; ?>
                            
                            <div class="col-sm-6">
                                <strong class="small text-muted">Stock:</strong>
                                <div>
                                    <span class="badge bg-<?= $product['stock_status']['class'] ?> me-1">
                                        <?= $product['quantity'] ?>
                                    </span>
                                    <?= $product['stock_status']['label'] ?>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Description -->
                <?php if ($product['has_description']): ?>
                <div class="card border-0 bg-light mb-3">
                    <div class="card-body">
                        <h5 class="card-title h6 mb-3">
                            <i class="bi bi-card-text text-primary"></i> Description
                        </h5>
                        <p class="mb-0"><?= nl2br(htmlspecialchars($product['description'])) ?></p>
                    </div>
                </div>
                <?php endif; ?>

                <!-- Propriétaire -->
                <?php if ($owner && !empty($owner['nom'])): ?>
                <div class="card border-0 bg-light mb-3">
                    <div class="card-body">
                        <h5 class="card-title h6 mb-2">
                            <i class="bi bi-person-circle text-primary"></i> Géré par
                        </h5>
                        <div class="d-flex align-items-center">
                            <div class="me-3">
                                <i class="bi bi-person-badge fs-4 text-muted"></i>
                            </div>
                            <div>
                                <div class="fw-semibold"><?= htmlspecialchars(trim($owner['prenom'] . ' ' . $owner['nom'])) ?></div>
                                <?php if (!empty($owner['email'])): ?>
                                <small class="text-muted"><?= htmlspecialchars($owner['email']) ?></small>
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
                <?php endif; ?>

                <!-- Fournisseurs -->
                <?php if (!empty($suppliers)): ?>
                <div class="card border-0 bg-light mb-3">
                    <div class="card-body">
                        <h5 class="card-title h6 mb-3">
                            <i class="bi bi-truck text-primary"></i> Fournisseurs
                            <span class="badge bg-secondary ms-2"><?= count($suppliers) ?></span>
                        </h5>
                        
                        <div class="suppliers-list">
                            <?php foreach ($suppliers as $supplier): ?>
                            <div class="supplier-item border-bottom pb-2 mb-2">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <div class="fw-semibold small text-dark">
                                            <?= htmlspecialchars(trim($supplier['prenom'] . ' ' . $supplier['nom'])) ?>
                                            <?php if ($supplier['is_primary']): ?>
                                            <span class="badge bg-primary ms-1">Principal</span>
                                            <?php endif; ?>
                                        </div>
                                        <?php if (!empty($supplier['telephone'])): ?>
                                        <small class="text-muted">
                                            <i class="bi bi-telephone"></i> <?= htmlspecialchars($supplier['telephone']) ?>
                                        </small>
                                        <?php endif; ?>
                                    </div>
                                    <div class="text-end">
                                        <?php if (($supplier['price'] ?? 0) > 0): ?>
                                        <div class="fw-bold text-success">
                                            <?= number_format($supplier['price'], 2, ',', ' ') ?> €
                                        </div>
                                        <?php endif; ?>
                                        <?php if (!empty($supplier['delivery_time'])): ?>
                                        <small class="text-muted"><?= $supplier['delivery_time'] ?> jours</small>
                                        <?php endif; ?>
                                    </div>
                                </div>
                                <?php if (!empty($supplier['notes'])): ?>
                                <small class="text-muted d-block mt-1">
                                    <i class="bi bi-chat-left-text"></i> <?= htmlspecialchars($supplier['notes']) ?>
                                </small>
                                <?php endif; ?>
                            </div>
                            <?php endforeach; ?>
                        </div>
                    </div>
                </div>
                <?php endif; ?>

                <!-- Actions -->
                <div class="action-buttons mt-4">
                    <?php if ($product['quantity'] > 0): ?>
                    <button type="button" class="btn btn-primary btn-lg w-100 mb-2">
                        <i class="bi bi-cart-plus"></i> Ajouter au panier
                    </button>
                    <?php else: ?>
                    <button type="button" class="btn btn-secondary btn-lg w-100 mb-2" disabled>
                        <i class="bi bi-x-circle"></i> Produit indisponible
                    </button>
                    <?php endif; ?>
                    
                    <div class="row g-2">
                        <div class="col">
                            <button type="button" class="btn btn-outline-primary w-100">
                                <i class="bi bi-heart"></i> Favoris
                            </button>
                        </div>
                        <div class="col">
                            <button type="button" class="btn btn-outline-secondary w-100">
                                <i class="bi bi-share"></i> Partager
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Informations techniques -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="card border-0 bg-light">
                <div class="card-body">
                    <h5 class="card-title h6 mb-3">
                        <i class="bi bi-clock-history text-primary"></i> Informations techniques
                    </h5>
                    <div class="row g-3 small text-muted">
                        <div class="col-md-6">
                            <strong>Créé le:</strong> <?= $product['created_date'] ?>
                        </div>
                        <?php if ($product['updated_at'] !== $product['created_at']): ?>
                        <div class="col-md-6">
                            <strong>Modifié le:</strong> <?= $product['updated_date'] ?>
                        </div>
                        <?php endif; ?>
                        <div class="col-md-6">
                            <strong>ID Produit:</strong> #<?= $product['id'] ?>
                        </div>
                        <div class="col-md-6">
                            <strong>Utilisateur:</strong> #<?= $product['user_id'] ?>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.product-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #1f2937;
    line-height: 1.3;
}

.main-product-image {
    transition: transform 0.3s ease;
}

.thumbnail-image {
    transition: all 0.2s ease;
}

.thumbnail-image:hover {
    transform: scale(1.05);
    border-color: #007bff !important;
}

.thumbnail-image.active {
    border-color: #007bff !important;
    box-shadow: 0 0 0 2px rgba(0,123,255,0.25);
}

.price-section .main-price {
    text-shadow: 0 1px 2px rgba(0,0,0,0.1);
}

.supplier-item:last-child {
    border-bottom: none !important;
    padding-bottom: 0 !important;
    margin-bottom: 0 !important;
}

.action-buttons .btn {
    font-weight: 600;
    transition: all 0.2s ease;
}

.action-buttons .btn:hover {
    transform: translateY(-1px);
}

@media (max-width: 768px) {
    .main-product-image {
        height: 250px !important;
    }
    
    .thumbnail-image {
        width: 60px !important;
        height: 60px !important;
    }
    
    .price-breakdown .col-4 {
        flex: 0 0 auto;
        width: 33.333333%;
    }
}
</style>

<script>
function changeMainImage(imageUrl, index) {
    const mainImage = document.getElementById('mainProductImage');
    if (mainImage) {
        mainImage.src = imageUrl;
    }
    
    // Mise à jour des miniatures actives
    document.querySelectorAll('.thumbnail-image').forEach((thumb, i) => {
        thumb.classList.toggle('active', i === index);
    });
}

// Support du swipe sur mobile pour la galerie
let touchStartX = 0;
let touchEndX = 0;
let currentImageIndex = 0;
const images = <?= json_encode(array_column($product['images'], 'url')) ?>;

function handleGesture() {
    if (touchEndX < touchStartX - 50) {
        // Swipe gauche - image suivante
        currentImageIndex = (currentImageIndex + 1) % images.length;
        changeMainImage(images[currentImageIndex], currentImageIndex);
    }
    
    if (touchEndX > touchStartX + 50) {
        // Swipe droite - image précédente
        currentImageIndex = (currentImageIndex - 1 + images.length) % images.length;
        changeMainImage(images[currentImageIndex], currentImageIndex);
    }
}

const mainImage = document.getElementById('mainProductImage');
if (mainImage && images.length > 1) {
    mainImage.addEventListener('touchstart', e => {
        touchStartX = e.changedTouches[0].screenX;
    });
    
    mainImage.addEventListener('touchend', e => {
        touchEndX = e.changedTouches[0].screenX;
        handleGesture();
    });
}
</script>